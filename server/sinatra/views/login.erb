<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="css/output.css" rel="stylesheet">
</head>

<body>
  <div class="bg-[url('/images/fondo3.jpg')] bg-cover bg-center min-h-screen">
    <!-- Overlay para mejorar el contraste en móviles -->
    <div class="bg-black bg-opacity-50 min-h-screen flex justify-center items-center px-4">
      <div class="bg-fun-blue-50 py-8 px-6 w-full max-w-sm sm:max-w-md rounded-md shadow-md">

        <!-- Mostrar mensaje de error, si existe -->
        <% if locals[:error] %>
        <div class="p-4 mb-4 text-sm text-blue-800 bg-blue-100 rounded-lg" role="alert">
          <span class="font-medium">Error:</span> <%= locals[:error] %>
        </div>
        <% end %>

        <!-- Logo centrado -->
        <div class="flex justify-center items-center mb-6">
          <div class="w-32 h-32 rounded-full border-4 border-fun-blue-500 flex items-center justify-center">
            <img class="h-24 w-24" src="images/logo.png" alt="Geogef">
          </div>
        </div>

        <!-- Título del formulario -->
        <h2 class="text-center text-2xl font-bold text-gray-900">Ingresar a tu cuenta</h2>

        <!-- Formulario -->
        <div class="mt-8">
          <form class="space-y-6" action="#" method="POST">
            <!-- Campo de correo -->
            <div>
              <label for="email" class="block text-sm font-medium text-gray-900">Dirección de correo</label>
              <div class="mt-2">
                <input id="email" name="email" type="email" required class="block w-full rounded-md border-gray-300 shadow-sm focus:ring-fun-blue-600 focus:border-fun-blue-600 sm:text-sm">
              </div>
            </div>

            <!-- Campo de contraseña -->
            <div>
              <div class="flex justify-between items-center">
                <label for="password" class="block text-sm font-medium text-gray-900">Contraseña</label>
                <a href="https://forms.gle/HRECHiUvWDBU6p6k7" class="text-sm font-semibold text-fun-blue-600 hover:text-fun-blue-500">¿Olvidaste tu contraseña?</a>
              </div>
              <div class="mt-2">
                <input id="password" name="password" type="password" required class="block w-full rounded-md border-gray-300 shadow-sm focus:ring-fun-blue-600 focus:border-fun-blue-600 sm:text-sm">
              </div>
            </div>

            <!-- Botón de ingreso -->
            <div>
              <button type="submit" class="w-full flex justify-center rounded-md bg-fun-blue-600 px-4 py-2 text-white font-semibold shadow-sm hover:bg-fun-blue-500 focus:outline-none focus:ring-2 focus:ring-fun-blue-600">Ingresar</button>
            </div>
          </form>

          <!-- Enlace para crear cuenta -->
          <p class="mt-6 text-center text-sm text-gray-500">
            No eres miembro? 
            <a href="/signup" class="font-semibold text-fun-blue-600 hover:text-fun-blue-500">Crear una cuenta</a>
          </p>
        </div>
      </div>
    </div>
  </div>
</body>

</html>
